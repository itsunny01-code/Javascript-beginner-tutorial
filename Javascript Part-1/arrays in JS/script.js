

let a = ['a','b','c',1,2,null]
console.log(Array.isArray(a)); // this is used for checking is that our array is array or not.
console.log(a);

// Push Or POp

a.push('d','e',1); // Push Is used for add a element in last
console.log(a);

a.pop(); // pop is used for deleting the last element
console.log(a);

a.unshift('good');  // unshift is used for add in the first element
console.log(a); 

a.shift();
console.log(a) // shift is used for deleting the first element


console.log(a[1]); // is used check the place of the element
