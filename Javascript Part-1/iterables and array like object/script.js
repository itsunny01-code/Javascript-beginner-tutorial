// iterables are those in which we can use for loop

const name = 'mynameisalokmishra';
for(const value of name){
    console.log(value);
}


// array like object is in which we can use a lenght property


const nam = 'mynameisalokmishra';
console.log(nam.length);